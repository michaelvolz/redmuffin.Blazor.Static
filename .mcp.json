{
  "servers": {
    "fetch": {
      "command": "docker",
      "args": [ "run", "-i", "--rm", "mcp/fetch" ]
    },
    "time": {
      "command": "docker",
      "args": [ "run", "-i", "--rm", "mcp/time" ]
    },
    "memory": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "-v",
        "claude-memory:/app/dist",
        "--rm",
        "mcp/memory"
      ]
    }
  }
}